/* eslint-disable arrow-body-style */
const util = require('./<%= filename %>.util');

describe('Utility - <%= name %>', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('TODO: should do unit test for <% name %>', () => {
    util();
  });
});

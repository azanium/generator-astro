/*
 * <%= filename %>.spec.js
 *
 * Created by <%= author %>
 * <%= date %>
 */

/* eslint-disable arrow-body-style */
const { <%= name %>Middleware } = require('./<%= filename %>.middleware');

describe('Middleware - <%= name %>Middleware', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('should pass request', (done) => {
    <%= name %>Middleware({}, {}, (err) => {
      expect(err).toBe(undefined);
      done();
    });
  });
});

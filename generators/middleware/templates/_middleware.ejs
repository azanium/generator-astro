/*
 * <%= filename %>.middleware.js
 *
 * Created by <%= author %>
 * <%= date %>
 */

/**
 * <%= capitalizedName %> Middleware
 *
 */
const <%= name %>Middleware = (req, res, next) => {
  next();
};

module.exports = {
  <%= name %>Middleware
};

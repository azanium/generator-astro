/* eslint-disable arrow-body-style */
const service = require('./<%= filename %>.service');

describe('Service - <%= service %>', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('TODO: should do unit test for <% service %>', () => {
    service();
  });
});

/* eslint-disable arrow-body-style */
// const controller = require('./<%= filename %>.controller');

describe('Test <%= name %>', () => {
  beforeEach(() => {});

  afterEach(() => {});

  test('TODO: should do unit test for <%= name %>', () => {
  });
});

/* eslint-disable arrow-body-style */

describe('Test <%= name %>', () => {
  beforeEach(async () => {
    beforeEach(() => {});
  });

  afterEach(() => {});

  it('TODO: should do unit test for <% name %>', () => {
  });
});

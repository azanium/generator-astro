/*
 * <%= filename %>.boot.js
 *
 * Created by <%= author %>
 * <%= date %>
 */

const { logger } = require('../../utils/logger');

const <%= name %>Boot = (app, next) => {
  logger.info('Starting <%= name %> boot task..');
  next();
};

module.exports = {
  <%= name %>Boot
};

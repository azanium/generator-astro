/* eslint-disable arrow-body-style */
const sinon = require('sinon');
const { <%= name %>Boot } = require('./<%= filename %>.boot');

describe('Boot - <%= name %>Boot', () => {
  let sandbox;

  beforeEach(async () => {
    sandbox = sinon.createSandbox();
  });

  afterEach(() => sandbox.restore());

  it('should run task and pass sequence to next boot task', () => {
    const nextSpy = sandbox.spy();
    const app = {};

    <%= name %>Boot(app, nextSpy);

    sinon.assert.calledOnce(nextSpy);
  });
});
